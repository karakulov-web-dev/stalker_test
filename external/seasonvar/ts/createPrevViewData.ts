export default function createPrevViewData () {
    let arr = [];
    for (let i = 0;i < 20;i++) {
      arr.push({
        name: "Идет Загрузка",
        poster: "posterPrevView"
      })
    }
    return arr;
  }